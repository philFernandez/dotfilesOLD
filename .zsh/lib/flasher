# flasher, flashes msg for duration

local duration=$1
local msg=$2
local i=0

clear

while [[ $i -lt $duration ]]; do
  echoti flash && \
    echo "\033[38;5;001m$msg"
  sleep 0.04
  ((i++))
done


# vi: ft=zsh
